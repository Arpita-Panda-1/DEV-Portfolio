export interface ContactFormData {
    name: string;
    email: string;
    message: string;
}

export interface ProjectType {
    title: string;
    desc: string;
}

export interface SkillType {
    name: string;
    icon: JSX.Element;
}