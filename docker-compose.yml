version: "3.9"

services:

  backend:
    build: ./server
    container_name: portfolio_backend
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    env_file:
      - ./server/.env

  frontend:
    build: ./client
    container_name: portfolio_frontend
    ports:
      - "5173:5173"